There are a few features that I'd like to add to adoctl:

- Add support for attachments on work items:
  List (implemented)
  Download (implemented)
  Upload
  Delete
- Add support for development links (commits, branches, and pull requests):
  The user may want to link the source feature branch.
  They may want to unlink abandoned PRs that ended up being handled with a different branch for the conflict resolution.
- Filter PR's by creation date.
